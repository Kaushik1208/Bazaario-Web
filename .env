# --- Database ---
# schema.prisma is now set to "postgresql". Replace this with your real
# connection string (e.g. from Neon or Supabase) before deploying.
# Example: postgresql://user:password@host:5432/dbname?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_6uTIgefxCOL1@ep-purple-butterfly-aecs70by-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
# For local dev without Postgres, you can temporarily switch the provider in
# prisma/schema.prisma back to "sqlite" and use DATABASE_URL="file:./dev.db".

# --- Auth ---
# Dev placeholder below — generate a real random secret before production,
# e.g. `openssl rand -base64 32`.
JWT_SECRET="bazaario-dev-secret-change-before-production-9f8a3c1e"

# --- OpenAI (optional) ---
# If unset, the AI engine automatically falls back to a deterministic rule-based
# recommendation engine so the full demo still works without an API key.
OPENAI_API_KEY=""

# --- Razorpay Test Mode (optional) ---
# If unset, the payment layer automatically falls back to a mock Razorpay client
# that simulates order creation / success / failure so the full demo still works
# without real Razorpay test keys.
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
RAZORPAY_WEBHOOK_SECRET=""

NEXT_PUBLIC_RAZORPAY_KEY_ID=""
